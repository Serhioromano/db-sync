# db-sync
DB ↔ DBML bidirectional sync tool with AI-friendly output. Snapshot any database to a human-readable DBML file, then apply changes back with minimal, safe ALTER operations. No migration history — the schema file IS the source of truth.
